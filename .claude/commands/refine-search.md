---
description: Roll accumulated decisions up into sharper fit criteria
argument-hint: "[optional: a specific adjustment to make]"
---

Tune the search based on what's been learned. Input: "$ARGUMENTS".

## Steps
1. **Review** all entries in `knowledge-base/decisions-log.md`, especially recent FIT/NO-FIT reactions, plus the current `knowledge-base/fit-criteria.md`.
2. **Find patterns:** factors Adam consistently rewards or rejects that the current rubric under- or over-weights. Look for new implicit dealbreakers or new positive signals.
3. **Propose changes** to the user first — show the specific edits (weight changes, new gates, band adjustments) and the evidence behind each. Don't silently rewrite the rubric.
4. **On approval,** update `knowledge-base/fit-criteria.md` (bump its "Last updated" date) and summarize the changes in the "Rolled-up patterns" section of `decisions-log.md`.
5. If the user passed a specific adjustment in "$ARGUMENTS", apply that directly (after confirming it's clear).
6. Commit the changes.

The rubric should get sharper every few roles. Bias toward small, evidence-backed tweaks over big rewrites.
